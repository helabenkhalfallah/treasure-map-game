/* eslint-disable no-unused-vars */
import {
  expect,
} from 'chai';
import TreasureMapUtils from '../TreasureMapUtils';

const {
  initTreasureMapGame,
  updateAdventurersMoves,
  generateGameOutput,
} = TreasureMapUtils;

describe('TreasureMapUtils Test', () => {
  it('initTreasureMapGame - case 1', () => {
    expect(true).to.equal(true);
  });
  it('updateAdventurersMoves - case 1', () => {
    expect(true).to.equal(true);
  });
  it('generateGameOutput - case 1', () => {
    expect(true).to.equal(true);
  });
});
